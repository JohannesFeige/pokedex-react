import { useCallback, useEffect, useState } from 'react'
import { PokedexResult } from '../types/pokemon'
import { get } from './api'

export const usePokedexApi = () => {
    // I would start by moving some code from the Pokedex.ts
    // (from pokedex-branch) into this file.
    // After that maybe some convinient variables are usefull too.
    // Maybe take a look at the todo in Pokedex.ts (this branch)

    return {}
}
